let newYearsResolutions = ['Keep a journal', 'Take a falconry class', 'Learn to juggle'];

console.log(newYearsResolutions);

// An array is a data structure that contains a list of items. Each item in an array has a numbered position, known as its index. Arrays are zero-indexed, which means the first item has an index of 0, the second item has an index of 1, and so on.
const hobbies = ['', '', ''];
console.log(hobbies)


// To access an item in an array, you can use bracket notation and the index of the item you want to access. For example, if you want to access the first item in the array, you would use the index 0.
const famousSayings = ['Fortune favors the brave.', 'A joke is a very serious thing.', 'Where there is love there is life.'];

const listItem = famousSayings[0];
console.log(listItem);
console.log(famousSayings[2]);
console.log(famousSayings[3]);


// Updating an array. 
let groceryList = ['bread', 'tomatoes', 'milk'];

groceryList[1] = 'avocados';

console.log(groceryList)

// Arrays with let and const
let condiments = ['Ketchup', 'Mustard', 'Soy Sauce', 'Sriracha'];

const utensils = ['Fork', 'Knife', 'Chopsticks', 'Spork'];

condiments[0] = 'Mayo';
console.log(condiments);

condiments = ['Mayo'];
console.log(condiments);

utensils[3] = 'Spoon';
console.log(utensils);


// Arrays with the length property
const objectives = ['Learn a new language', 'Read 52 books', 'Run a marathon'];

console.log(objectives.length);

// Arrays with the push() method
const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('mop floor', 'vacuum floor');

console.log(chores);

// Array with the pop() method
const chores = ['wash dishes', 'do laundry', 'take out trash', 'cook dinner', 'mop floor'];

chores.pop();

console.log(chores);


// Arrays with the shift() and unshift() methods
const groceryList = ['orange juice', 'bananas', 'coffee beans', 'brown rice', 'pasta', 'coconut oil', 'plantains'];

// The shift() method removes the first item of an array and returns that removed item. This method changes the length of the array.
groceryList.shift();
console.log(groceryList);

// The unshift() method adds one or more elements to the beginning of an array and returns the new length of the array.
groceryList.unshift('popcorn');
console.log(groceryList);

// Arrays with the slice() method. The slice() method returns a shallow copy of a portion of an array into a new array object selected from start to end (end not included). The original array will not be modified.
console.log(groceryList.slice(1, 4));

console.log(groceryList);

// Arrays with the indexOf() method. The indexOf() method returns the first index at which a given element can be found in the array, or -1 if it is not present.
const pastaIndex = groceryList.indexOf('pasta');

console.log(pastaIndex);

// Arrays and Functions. 
const concept = ['arrays', 'can', 'be', 'mutated'];

function changeArr(arr){
  arr[3] = 'MUTATED';
}

changeArr(concept);
console.log(concept);

const removeElement = (newArr) => {
  newArr.pop()
}

removeElement(concept);
console.log(concept);


// Nested Arrays. 
const numberClusters = [[1, 2], [3, 4], [5, 6]];

const target = numberClusters[2][1];